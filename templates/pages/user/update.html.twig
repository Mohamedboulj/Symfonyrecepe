{% extends "base.html.twig" %}
{% block body %}
    <div class="container">
        <div class="mt-2 alert alert-danger">
            {% for message in app.flashes('failure') %}
                <h5>{{ message }}</h5>
            {% endfor %}
        </div>
        <h2 class="mt-2">Update informations </h2>
        <div class="container mt-2">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
