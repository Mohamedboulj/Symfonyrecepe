<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Sym Recipe</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href={{ path('ingredient.list') }}>Ingredients
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href={{ path('recipe.index') }}>Recipes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href={{ path('ingredient.new') }}>New ingredient
                        </a>
                    </li>
                {% endif %}
                <div class="d-flex">
                    <ul class="navbar-nav me-auto">
                        {% if app.user %}
                            <li class="nav-item dropdown " style="margin-right: 5rem;">
                                <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button"
                                   aria-haspopup="true" aria-expanded="false">{{ app.user.fullName }}</a>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="{{ path('security.update',{id: app.user.id}) }}">modifier
                                        les
                                        informations</a>
                                    <a class="dropdown-item" href="{{ path('reset.password',{id: app.user.id}) }}">reinitialiser
                                        le mot de
                                        passe</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="{{ path('security.logout') }}">se deconnecter</a>
                                </div>
                            </li>
                        {% else %}
                            <li class="nav-item"><a class="nav-link " href={{ path('security.login') }}>Se
                                    connecter
                                </a></li>
                            <li class="nav-item"><a class="nav-link " href={{ path('register') }}>S'enregistrer</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </ul>
        </div>
    </div>
</nav>


